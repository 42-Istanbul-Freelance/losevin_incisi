services:
  losev:
    build: .
    container_name: losev-app
    ports:
      - "3000:3000"
    volumes:
      - losev-data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/dev.db
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  losev-data:
